# Optional: full URL to token server (bypasses Vite proxy). Leave unset for local dev.
# VITE_VOICE_SERVER_URL=http://localhost:3001

# Opens in new tab from "Dashboard" in the sidebar
VITE_DASHBOARD_URL=https://your-app.up.railway.app/dashboard
